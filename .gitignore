# Logs
*.log
npm-debug.log*

node_modules/
dist/
dist-ssr/
build/
*.local
.env
backend/.env
backend/prisma/dev.db
backend/prisma/dev.db-journal
backend/prisma/test.db
backend/prisma/test.db-journal

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Claude Code memory (local only)
memory/

# Claude Code worktrees + local-only settings (NOT pushed; agents/ IS tracked)
.claude/worktrees/
.claude/settings.local.json
.claude/*.local.json

# Local env variants & build/test caches (never push)
.env.*.local
.env.local
*.tsbuildinfo
.eslintcache
coverage/

# Ephemeral per-session working notes (kept local; the DEVLOG is the record)
session-handoff.md
/*-handoff.md

# Literature corpus — copyrighted publisher PDFs, kept local only (NOT pushed).
# Cataloged in docs/literature-index.md; the renamed/ folder holds local symlinks.
Projektseminar_Literatur/
# Course material binaries at repo root (local only). Generated decks in
# docs/slides/ are intentionally NOT ignored.
/PP-Master-FAU_WiSo_16zu9_v2.pptx
/Projektseminar Wi1 Gruppe 5 - NuernbergBarrierefrei.pdf

# LaTeX build artifacts
docs/reports/*.aux
docs/reports/*.log
docs/reports/*.out
docs/reports/*.toc

# Team-meeting prep notes (internal, local only)
docs/team-meeting/

# Source material for the Facebook import (images live in Supabase Storage, not git)
Posts_Kokkali_Facebook/

# Source material for the entity-image import (Events/Projects/News/Wissen) and
# branding logos. Entity images live in Supabase Storage; logos are also bundled
# into public/ + src/assets. The raw source folder is local-only (NOT pushed).
Bilder_Events_Projekte_News/
