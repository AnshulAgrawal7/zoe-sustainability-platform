# Night Run — 2026-06-12 — `feat/landing-overhaul`

Autonomer Lauf. Branch `feat/landing-overhaul` von `main` (HEAD `283811a`).
Modus: vollautonom, keine Rückfragen. Pro Aufgabe ein Commit (Conventional, kein
Co-Author-Trailer). Gating pro Aufgabe: `tsc` (FE+BE type-check) · `eslint` ·
`vite build` · FE-Tests (Vitest). **BE-Tests & E2E sind in dieser Umgebung
NICHT lauffähig** (kein lokaler Postgres / kein Docker — siehe Memory
`test-setup`), daher als Gate durch `backend type-check` ersetzt und hier
vermerkt. Niemals mit rotem Build committen.

Baseline vor Start: FE `tsc` clean · `eslint` clean · Vitest 22/22 grün ·
Supabase Storage erreichbar (Bucket `post-images`, public) · Supabase Prod-DB
über Prisma erreichbar.

---

## Statustabelle

| Aufgabe | Status | Commit | Notiz |
|---|---|---|---|
| (Setup) Branch + Log | DONE | db7470e | Branch + Logdatei angelegt |
| D1 Bild-Import (Entities) | DONE | a3767a7 | 22/22 gematcht, idempotent, gegen Prod angewandt |
| A1 Seiten-Logo + Favicon | DONE | e0baf50 | Logo-Icon im Header + PNG-Favicon; Datei vorhanden → kein Text-Fallback nötig |
| A2 Gemeinde-Logo im Banner | DONE | 1381a9e | Wappen ersetzt Text in Landing-Hero-Badge (weißes Pill für Kontrast), alt=Gemeindename |
| A3 Sprachumschalter (nur Flaggen) | DONE | 2d34622 | Reihenfolge EL→EN→DE, Codes entfernt, aria-label/aktiv behalten, ≥44px Touch (md) |
| A4 Navbar DE Termine→Veranstaltungen | DONE | d6b7cd6 | EN/EL bereits Events/Εκδηλώσεις |
| A5 Logout-Toast | DONE | 04eb12b | Neuer toastStore + Toaster (aria-live, auto-dismiss 4s + manuell schließbar); DE „Erfolgreich ausgeloggt" |
| B1 Landing Section-Reihenfolge | DONE | 75250c8 | Hero→Projekte→Veranstaltungen→Kennzahlen→News→Lernen→Was-ist-ZOE→Community→CTA; Hintergründe re-alterniert |
| B2 Kennzahlen (3 KPIs, grünes Banner) | DONE | (this) | 640 Freiwillige / 2.389 Bäume / 127 Schlüpflinge; Quellen entfernt; Microcopy; Intl-Locale-Format; dynamischer projectCount entfernt |

Legende: DONE · BLOCKED · SKIPPED · PARTIAL

---

## Entscheidungen

- **BE-/E2E-Tests:** Umgebung hat keinen lokalen Postgres/Docker → BE-Test-Suite
  nicht ausführbar. Gate dafür: `backend npm run type-check`. In jeder Aufgabe,
  die Backend berührt, vermerkt.
- **News-Ordner → `Post`-Modell (nicht `FeedPost`):** Die Dateinamen im Ordner
  `News/` (Forum_der_Kreislauf, Schutz_von_Meeresschildkroeten, LED) matchen
  exakt auf die drei `Post`-Zeilen (Projekt-Lifecycle-News), nicht auf die
  importierten griechischen FeedPosts (FB-Folders 1–20, unverwandte Titel). Das
  `Post`-Modell ist via feedController-Adapter Teil des öffentlichen Feeds, also
  erscheinen die Bilder dort. needsReview-/Übersetzungs-Pipeline der FeedPosts
  bleibt unangetastet.
- **Logos & Favicon (A1/A2):** Werden in Supabase Storage hochgeladen (Pflicht
  laut Aufgabe, idempotent, URLs im Report) UND zusätzlich als lokale Assets ins
  Repo gelegt (public/ + src/assets), damit Build/Tests/Dev nicht von Prod-Storage
  abhängen. Frontend referenziert die lokalen Assets.

---

## EL-REVIEW offen

EL wurde überall eingetragen (keine EN-Platzhalter), aber folgende von mir selbst
verfassten griechischen Strings sind nicht muttersprachlich geprüft — bitte
gegenlesen:

- `landing.stats.heading` = „Η ZOE σε αριθμούς"
- `landing.stats.disclaimer` = „Πλασματικά δεδομένα επίδειξης — αυτό το πρωτότυπο χρησιμοποιεί επινοημένες τιμές."
- `landing.facts.volunteers.label` = „εθελοντές ήδη ενεργοί σε όλη τη Βόρεια Κέρκυρα"
- `landing.facts.trees.label` = „ντόπια δέντρα & ελιόδεντρα φυτεύτηκαν"
- `landing.facts.turtles.label` = „νεοσσοί χελώνας οδηγήθηκαν με ασφάλεια στη θάλασσα"

---

## BILD-MATCHING-REPORT

Skript: `backend/scripts/import-entity-images.ts` (idempotent). Bucket
`entity-images` (public, neu angelegt). Re-Run-Befehl:
`cd backend && node --env-file=.env -r ts-node/register/transpile-only scripts/import-entity-images.ts`
(`--report-only` für Trockenlauf). Zweiter Lauf = 22 idempotente Skips, 0 Writes.

**Heuristik:** Dateinamen sind deutsch/sprechend, DB-Titel mehrsprachig (EN/EL/DE)
→ rein automatischer Normalisierungs-Match unzuverlässig (z. B. „Green_Mov" ↔
„GreenMove — Sustainable Mobility"). Daher reviewte Datei→ID-Tabelle im Skript,
zur Laufzeit gegen die Live-DB verifiziert und geloggt.

**GEMATCHT & GESETZT (22/22):**
- Projects/ → Project.imageUrl (7): Natural_Monuments→proj-natural-monuments ·
  Maine_Protection_Seaturtles→proj-marine · Antinioti_lagoon→proj-antinioti ·
  Drinking_Water→proj-water-quality · Environmental_Education→proj-education ·
  Green_Mov→proj-greenmove · Circular_Economy_Network→proj-circular
- Events/ → Event.imageUrl (8): Jugend_Umweltworkshop→evt-youth-eco ·
  Schulung_Wasser-Monitoring→evt-water-monitoring · Jaehrliches_SDG_Forum→evt-sdg-forum ·
  Biodiverstitaets_Spaziergang→evt-biodiversity-workshop ·
  Planung_der_Gemeinschaftskompostierung→evt-composting-expansion ·
  Kuestenreinigungstag_Nordkorfu→evt-cleanup-jun25 · Zweite_Kuestenreinigung→evt-cleanup-jul25 ·
  Tag_der_offenen_Tuer→evt-recycling-hub
- Wissen/ → LearningResource.imageUrl (4): Wie_Recycling→learn-circular ·
  Die_Antinioti_Lagune→learn-antinioti · Meeresschildkroeten→learn-marine · Erimitis→learn-erimitis
- News/ → Post.imageUrl (3): Forum_der_Kreislauf→post-circular-forum ·
  Schutz_von_Meeresschildkroeten→post-new-marine · LED→post-completed-led

**LOGOS (Branding-Bucket-Pfad `entity-images/branding/`):**
- Logo_Icon_transparent.png → `…/branding/logo-icon.png` (A1)
- Logo_Gemeinde_Korfu.png → `…/branding/logo-gemeinde-korfu.png` (A2)
- Zusätzlich lokal gebündelt: `src/assets/logo-icon.png`,
  `src/assets/logo-gemeinde-korfu.png`, `public/favicon.png`,
  `public/apple-touch-icon.png` (downscaled via ImageMagick).

**NICHT GEMATCHT (Dateien): keine** (alle 22 Quelldateien zugeordnet).

**ENTITÄTEN OHNE BILD (nach Import):**
- `proj-zoe-programme` (unlisted Umbrella — bewusst ohne Bild, korrekt)
- `evt-reforestation-day` (keine Quelldatei vorhanden — echter Fehlbestand)
- 9× E2E-Test-Events (`cmq9…`) — werden in G1 entfernt

**Prod-Status:** Uploads + imageUrl-Writes sind ADDITIV und wurden direkt gegen
Prod-Supabase ausgeführt (laut Regeln erlaubt). Kein PENDING für D1.

---

## PENDING — MANUELLER PROD-DEPLOY

(Exakte Befehle/SQL für Schema-Migrationen & Seed-Datenmutationen, die NICHT
automatisch gegen Prod ausgeführt wurden.)
